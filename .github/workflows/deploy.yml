name: Deploy to GitHub Pages

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build with Vite
        run: |
          npm run build
          # Create the dragonfly-expense-hub directory in dist
          mkdir -p dist/dragonfly-expense-hub
          # Copy all files from dist to the subdirectory
          cp -r dist/* dist/dragonfly-expense-hub/
          # Copy PWA files to the correct location
          cp -r public/icon-192x192.png dist/dragonfly-expense-hub/
          cp -r public/icon-512x512.png dist/dragonfly-expense-hub/
          cp -r public/screenshot-desktop.png dist/dragonfly-expense-hub/
          cp -r public/screenshot-mobile.png dist/dragonfly-expense-hub/
          cp -r public/favicon.ico dist/dragonfly-expense-hub/
          cp -r public/site.webmanifest dist/dragonfly-expense-hub/
          # Move everything to the root for GitHub Pages
          cp -r dist/dragonfly-expense-hub/* .
        env:
          NODE_ENV: production
          VITE_BASE_URL: /dragonfly-expense-hub/
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4 